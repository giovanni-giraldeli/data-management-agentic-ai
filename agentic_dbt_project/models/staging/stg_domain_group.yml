version: 2

models:
  - name: stg_domain_group
    description: Staging model for domain group associations, filtering for currently active records.
    columns:
      - name: domain_group_id
        description: Unique identifier of the domain group.
        tests:
          - unique
          - not_null
      - name: customer_id
        description: Unique identifier of a customer.
        tests:
          - not_null
      - name: dw_valid_from
        description: The timestamp from which the record is valid in the data warehouse.
      - name: dw_valid_to
        description: The timestamp until which the record is valid in the data warehouse.
